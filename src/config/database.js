// const dotenv = require("dotenv");
// dotenv.config();

module.exports = {
    dialect: "mysql",
    host: "localhost",
    username: "aluno_medio",
    password: "@lunoSenai23.",
    database: "thoughts",
    define: {
        timestamps: false,
        underscored: true
    }
}